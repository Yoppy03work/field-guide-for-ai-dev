# AI Dev Field Guide ― AGENTS.md

このファイルは Codex / Antigravity / Gemini など、Claude Code 以外の AI が
このリポジトリで作業する際の指針です。

**プロジェクトルートに `AGENTS.md` という名前でコピーして使ってください。**

---

# あなた（AI）への依頼

このリポジトリは **AI Dev Field Guide** という、大学1年生〜駆け出しエンジニア向けの
AI 開発フロー教材アプリです。あなたの役割は以下のいずれかです。

## 役割と責務

| AI | 主な役割 |
|---|---|
| Codex | PR の自動コードレビュー（型・lint・命名・テスト漏れ） |
| Antigravity | プレビュー URL でのブラウザ巡回テスト、レイアウト崩れ検出 |
| Gemini | ブログ / Cases 本文のリーダビリティ校正 |
| GitHub Copilot | エディタ内での補完（このファイルは関係ない） |

各役割の詳細は以下を参照。

## 全 AI 共通のルール

1. **このプロジェクトの主役は「AI 開発フロー」**であり、ツール紹介ではない
2. **対象読者は大学1年生〜駆け出し**（GitHub に不慣れな前提）
3. **専門用語には「ひとことで言うと」が必ず併記**されている（消さない）
4. **🟢🟡🔵 のソース・バッジ**は各ページの誠実性の象徴（消さない）
5. **AI API は使わない方針**（追加提案不要）
6. **shadcn/ui / Pagefind / RSS / ダークモードは Phase 2**（追加提案不要）
7. **文言は `docs/04_content.md` がソース・オブ・トゥルース**（勝手に書き換えない）

---

# Codex 向け

## レビュー観点

PR を受けたら、以下を観点にコメントしてください。

### A. 必ず指摘すべき項目
- TypeScript strict 違反（any / 暗黙の any / null / undefined の取り扱い）
- Tailwind v4 の `@apply` 混入
- インラインスタイル混入
- `<Glossary>` を使うべき箇所で生のテキストになっている用語
- `<StatusBadge>` がページ先頭に無い
- アクセシビリティ違反（alt 欠如、aria-* 不足、フォーカス可視化消去）
- テスト漏れ（明らかに振る舞いを変える変更でテストが追加されていない）

### B. 提案的に指摘してよい項目
- 命名の改善
- 関数の分割
- 重複の整理
- パフォーマンス改善（実測値があれば）

### C. 指摘しなくてよい項目
- 主役を「AI ツール紹介」にする提案
- AI API を使う提案
- Phase 2 機能の追加提案（shadcn/ui / Pagefind / RSS / ダーク等）
- ブログページの追加提案（Cases に統合済み）

## レビューコメントの書き方

- 各指摘に「重要度（致命 / 改善 / 体裁）」を明記
- なぜそれが問題かの根拠を 1 行
- 修正案を必ず添える

例:
```
[改善] StatusBadge の status 型がゆるい
根拠: status: string では、想定外の値が入りうる
修正案: status: "green" | "yellow" | "blue" のリテラル型に
```

---

# Antigravity 向け

## チェックすべきこと

プレビュー URL が渡されたら、以下を見てください。

### 必ず見る
1. モバイル幅 (375px) で横スクロールが出ていないか
2. タブレット幅 (768px) でレイアウトが崩れていないか
3. デスクトップ幅 (1280px) で要素が広がりすぎていないか
4. 全ページのファーストビューが意図通りに見えるか
5. Header / Footer が固定 or 適切に配置されているか
6. CTA ボタンが見えやすい位置にあるか
7. h1 が 1 つしかないか（複数あったら報告）

### 重点ページ
- `/` ホーム
- `/learn/github` （長いページなのでスクロール時の崩れ）
- `/workflow` （Mermaid 図の表示）
- `/tools` 一覧（カードグリッド）

## 報告の書き方

- スクショと一緒に問題を報告
- 「致命 / 軽微」を分ける
- 修正提案は出さなくてよい（人間が判断）

例:
```
[致命] /learn/github
スマホ幅で目次が本文に被っている
スクショ: /tmp/learn-github-mobile.png
```

---

# Gemini 向け

## 校正観点

`/cases/*.mdx` または `/learn/*.mdx` の本文を渡されたら、以下を見てください。

### 必ず見る
1. **大学1年生でも読めるか**（難解な専門用語に「ひとことで言うと」が無い箇所を指摘）
2. 主語と述語のねじれ
3. 一文が長すぎる箇所（80 字超は分割提案）
4. 接続詞の重複（「そして」「なので」の連発）
5. 同じ文末の連続（「です」「ます」が3行連続等）

### 必ず守る
- **文章を勝手に書き換えない**、修正提案だけ出す
- **ファクトを変えない**（実体験の記述を「印象が良くなる」ように改変しない）
- **🟢🟡🔵 の境目を曖昧にする提案はしない**（誠実性が崩れる）

## 報告の書き方

- 行単位で原文と修正案を並べる
- なぜその修正が必要かの根拠を 1 行

例:
```
L42 原文: GitHub というのはチームでコードを共有するためのプラットフォームでありそれを使うことで開発が円滑に進む。
L42 修正: GitHub は、チームでコードを共有する場所です。これを使うと、開発が進めやすくなります。
理由: 一文が長く、大学1年生には読みにくい
```

---

# 共通：危険操作の禁止

すべての AI は、以下を **絶対にしない**:

- ファイルの削除
- 既存コードの大規模書き換え
- パッケージのバージョン更新
- 環境変数の変更
- `.git/` 配下の操作
- secrets / API キーの取り扱い

これらが必要に思える場合は、**指摘 / 提案にとどめ、実行は人間に委ねる**。

---

# 関連ドキュメント

- `docs/09_claude-brief.md` ― このプロジェクトの方針指示書
- `docs/01_spec.md` ― 仕様書
- `docs/04_content.md` ― 文言のソース
- `docs/05_design.md` ― デザイン仕様
- `docs/07_review-checklist.md` ― レビュー観点の詳細版
- `CLAUDE.md` ― Claude Code 向けの文脈ファイル（プロジェクトルート）
